import "./main.css";
import React, { useState } from "react";
/*
function Chatbot() {
  const [chatOpen, setChatOpen] = useState(false); //변수이름 선언 chatOpen 등
  return (
    <div className={`Chatbot ${chatOpen ? "open" : "closed"}`}>
      <button onClick={() => setChatOpen(!chatOpen)}>
        {chatOpen ? "Close Chat" : "Open Chat"}
      </button>
      {chatOpen && <div className="chat-window">Chat Window</div>}
    </div>
  );
}
*/